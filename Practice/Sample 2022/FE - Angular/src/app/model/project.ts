export interface Project{
  // id (int), ProjectManagerID (int), name (string), description (string),   members(string)
  id: number;
  projectManagerID: number;
  name: string;
  description: string;
  members: string;
}
